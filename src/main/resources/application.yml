spring:
  application:
    name: jobport

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect   # (?? ???? ??)

  liquibase:
    change-log: classpath:/db/migration.yml
#    enabled: false